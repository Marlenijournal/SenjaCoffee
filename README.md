# 1. Init project & install 
npm init -y

# 2. Backend dependencies
npm install express cors pg dotenv bcryptjs jsonwebtoken multer helmet compression

# 3. Frontend dependencies (Vite + React)
npm install react react-dom react-router-dom axios react-icons react-hot-toast swiper

# 4. Dev dependencies
npm install -D vite @vitejs/plugin-react nodemon concurrently
